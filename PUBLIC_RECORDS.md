# Public Records

AquaStat now provides facility-specific public-record request templates at:

- `/api/v1/facilities/{facilityId}/public-records/templates`

These templates:

- identify likely record holders
- request institution-level rather than personal records
- preserve the distinction between projected, permitted, allocated, and actual use
- avoid any suggestion of bypassing confidentiality or access controls
