# AquaStat Python SDK

The AquaStat Python SDK is a lightweight client for AquaStat API deployments.

It provides:

- sync and async-friendly `httpx` usage patterns
- configurable base URLs for self-hosted or hosted AquaStat environments
- normalized client behavior for footprint and scheduling workflows
