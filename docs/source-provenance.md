# Source Provenance

Every important facility field can link back to evidence metadata including source ID, source type, extraction method, verification status, confidence, and notes. AquaStat preserves evidence separately from merged record values so reconciled numbers remain auditable.
