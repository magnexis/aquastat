# Organization Model

Organizations are tracked separately from facilities so operator, owner, developer, and tenant roles can diverge over time. The current development fixtures model operator relationships and facility lists without assuming all roles collapse to one entity.
