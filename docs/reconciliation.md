# Reconciliation

Reconciled facility estimates select the strongest available value while preserving alternatives in provenance and history. Planned and operational values remain separate, and synthetic conflicting-capacity fixtures are included under `tests/fixtures/reconciliation/`.
