# Verification

Facility-level verification labels describe the record, not every field. AquaStat keeps field-level statuses such as `reported`, `documented`, `parsed`, `estimated`, and `disputed` so a facility can be source-linked while some metrics remain inferred.
