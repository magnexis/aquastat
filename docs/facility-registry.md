# Facility Registry

AquaStat Phase 3 introduces a normalized facility registry layer built around synthetic development fixtures and field-level provenance. Facilities keep nullable typed fields, explicit verification status, source summaries, and coverage labels so unknown values remain unknown instead of being flattened to zero.
