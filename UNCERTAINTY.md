# Uncertainty

AquaStat keeps uncertainty visible rather than collapsing every output to one precise number. Operational estimates widen when:

- facility figures are synthetic or incomplete
- values are planned rather than operational
- WUE is estimated rather than measured
- direct water data is unavailable

Official and derived figures remain labeled separately from modeled results.
