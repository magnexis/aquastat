# API Versioning

AquaStat's primary versioned surface is `/api/v1`. Compatibility routes under `/api/v2` remain available for features added in earlier phases.

No legacy unversioned estimate routes were removed in this phase.
