# Model Registry

The current registry exposes one stable model:

- `aquastat-water-intelligence` version `2.0.0`

The registry is viewable through the control center and `/api/v1/control-center/models`.
